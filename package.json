{
  "name": "space-invaders-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "jest --verbose --runInBand",
    "tar": "git archive --format tar --output ./deploy.tar main",
    "tar-append": "tar --file=./deploy.tar --append .env*",
    "deploy": "yarn tar && yarn tar-append && caprover deploy -t ./deploy.tar"
  },
  "author": "Erwin Cabrera",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^6.6.5",
    "mongoose-unique-validator": "^3.1.0"
  },
  "devDependencies": {
    "jest": "^29.2.1",
    "nodemon": "^2.0.20"
  }
}
